# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: biome
      run: bunx biome ci .

    - name: typecheck
      run: bun x tsc --noEmit

    - name: test
      run: bun test

pre-push:
  jobs:
    - name: rebase
      run: git pull --rebase origin main
